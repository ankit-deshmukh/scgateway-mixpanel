# Uncomment the next line to define a global platform for your project
platform :ios, '11.0'

target 'SCGateway-Mixpanel' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  # Pods for SCGateway-Mixpanel
  project './SCGateway-Mixpanel.xcodeproj'
  pod 'Mixpanel-swift','4.0.2'
#  pod 'Auth0', '~> 2.0'
end
